export namespace Enums {
  export enum ErrorCode {
    UNAUTHORIZED = 401,
  }

  export enum Redirect {
    HOME = '/',
    SERVICE = '/service',
  }

  export enum RouteRoot {
    HOME = '/',
    JOIN = '/join',
    SERVICE = '/service',
    PROXY = '/proxy',
    FONT = '/fonts',
    SVG = '/svg',
  }
}
